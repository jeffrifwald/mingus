#!/usr/bin/env node

var path = require('path');
var child_process = require('child_process');


var execPath, args, proc;

if (process.argv[2] === 'cover') {
    execPath = require.resolve('babel/bin/babel-node');
    args = [].concat([
        require.resolve('babel-istanbul/lib/cli'),
        'cover',
        require.resolve('mocha/bin/_mocha')
    ], process.argv.slice(3));
} else if (process.argv[2] === 'check-coverage') {
    execPath = require.resolve('babel-istanbul/lib/cli');
    args = [].concat([
        'check-coverage'
    ], process.argv.slice(3));
}

if (execPath && args) {
    proc = child_process.spawn(execPath, args, {stdio: 'inherit'});
    proc.on('exit', function (code, signal) {
        process.on('exit', function() {
            if (signal) {
                process.kill(process.pid, signal);
            } else {
                process.exit(code);
            }
        });
    });
    process.on('SIGINT', function () {
        proc.kill('SIGINT');
        proc.kill('SIGTERM');
    });
}
